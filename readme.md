loading="eager" → l’image est chargée immédiatement (comportement par défaut pour les images critiques).
loading="auto" → le navigateur choisit la stratégie (souvent “eager” par défaut).
Pour empêcher qu’une image se charge automatiquement, on utilise loading="lazy" dans la balise <img>.